/*
import { getDbHost, getDbName, getDbPassword, getDbUser } from "../helpers/env";

export const configDb = {
  HOST: getDbHost(),
  USER: getDbUser(),
  PASSWORD: getDbPassword(),
  DB: getDbName(),
  dialect: "postgres",
  pool: {
    max: 5,
    min: 0,
    acquire: 30000,
    idle: 10000,
  },
};
*/
