version: "3.7"

services:
  api:
    build: ./api
    command: npm run start
    restart: unless-stopped
    ports:
      - 4000:4000
    environment:
      - PORT=4000
      - DB_URL=mongodb://api_db:27017
      - REDIS=redis://cache
      - EMAIL=bugrom@mail.ru
      - API_KEY_MAILGAN=715cdc72939ad4e54d208750932ced63-d32d817f-af12a5fa
      - DOMAIN_MAILGAN=sandboxc3e85056266e4db2a2a62f6ed9078b3d.mailgun.org
    depends_on:
      - redis_db
      - api_db
    networks:
      - invoice-network

  redis_db:
    image: redis:latest
    container_name: cache
    expose:
      - 6379
    networks:
      - invoice-network

  api_db:
    image: mongo:latest
    container_name: invoice-api-db
    ports:
    - 27017:27017
    networks:
      - invoice-network

networks:
  invoice-network:
    driver: bridge
